services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: bingo
      POSTGRES_PASSWORD: bingo
      POSTGRES_DB: bingo
    ports:
      - "5432:5432"
    volumes:
      - bingo_pg_data:/var/lib/postgresql/data

volumes:
  bingo_pg_data:

